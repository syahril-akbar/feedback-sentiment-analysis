feat: Enhance data processing and robustness

This commit introduces several improvements to standardize data handling, fix bugs, and improve the reliability of file I/O and visualization.

- **Data Consistency**:
  - Renamed column "komentar" to "kritik dan saran" consistently across all relevant scripts (`main.py`, `evaluation.py`, `utils.py`, `visualization.py`).

- **File Handling**:
  - Updated `sentiment_lexicon.py` to correctly parse tab-separated lexicon files.
  - Enhanced `utils.py` to automatically create output directories before writing, preventing potential `FileNotFoundError`.
  - Forced pandas to read all Excel data as strings to avoid incorrect data type inference.

- **Visualization & Output**:
  - Adjusted the cluster count plotting logic in `visualization.py` for a more accurate representation.
  - Fixed Unicode encoding errors in console print statements to ensure proper character display.
